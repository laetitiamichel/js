{
  "name": "js",
  "version": "1.0.0",
  "description": "vérifier la version npm\r puis npm init\r npm install --save-dev babel-cli babel-preset-env",
  "main": "index.js",
  "scripts": {
    "build": "babel --no-babelrc src -w -d js --preset=env"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
